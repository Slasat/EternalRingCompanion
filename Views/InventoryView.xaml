<UserControl x:Class="EternalRingCompanion.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="34,26,34,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Inventory" Style="{StaticResource H1}"/>
                <TextBlock Style="{StaticResource Muted}" Margin="0,3,0,0"
                           Text="Set how many of each item, ring or gem you carry. Weapons and keys use 1 for owned."/>
                <Border Style="{StaticResource Rule}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button x:Name="FillBtn" Content="Fill category" Style="{StaticResource GhostButton}" Margin="0,0,8,0" Click="OnFill"/>
                <Button x:Name="ClearBtn" Content="Clear category" Style="{StaticResource GhostButton}" Click="OnClear"/>
            </StackPanel>
        </Grid>

        <ItemsControl x:Name="CategoryBar" Grid.Row="1" Margin="0,0,0,6">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <TextBlock x:Name="Status" Grid.Row="2" Style="{StaticResource Muted}" Margin="0,2,0,10"/>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Padding="0,0,10,20">
            <Border Style="{StaticResource Card}" MaxWidth="720" HorizontalAlignment="Left">
                <StackPanel x:Name="Rows"/>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
