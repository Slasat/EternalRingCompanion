<UserControl x:Class="EternalRingCompanion.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="34,26,34,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Player Stats" Style="{StaticResource H1}"/>
                <TextBlock Style="{StaticResource Muted}" Margin="0,3,0,0"
                           Text="Health, magic, and your name."/>
                <Border Style="{StaticResource Rule}"/>
            </StackPanel>
            <Button x:Name="RefillBtn" Content="Restore HP &amp; MP" Style="{StaticResource GhostButton}"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Click="OnRefill"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,20">
            <StackPanel x:Name="Root" MaxWidth="720" HorizontalAlignment="Left"/>
        </ScrollViewer>
    </Grid>
</UserControl>
